version: "3"
services:
  db:
    image: postgres:11-alpine
    enviroment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: workout
    ports:
      - "5432:5432"